export const initialState = {
    note: 'Learn about reducers',
    noteToggle: false
}

export const reducer = (state, action) => {
    switch (action.type) {

    default:
        return state
    }
}
