export const initialState = [{
    note: '',
    noteToggle: false,
    id: ''
}]

export const noteReducer = (state, action) => {
    switch (action.type) {

     default:
        return state
    }
}